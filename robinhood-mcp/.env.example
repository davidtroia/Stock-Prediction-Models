# Robinhood credentials
ROBINHOOD_USERNAME=your_email@example.com
ROBINHOOD_PASSWORD=your_password

# Optional: TOTP code for accounts with 2FA enabled
# If you use an authenticator app, generate a code and set it here before starting the server.
# For automated use, consider generating a device token instead (see README).
ROBINHOOD_MFA_CODE=
